name: CI Build
     
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
 
jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    - name: Install dependencies
      run: dotnet restore --framework net7 Chorn.AiPullRequestAssistor\Chorn.AiPullRequestAssistor.csproj
    - name: Build
      run: dotnet build --framework net7 --configuration Release --no-restore Chorn.AiPullRequestAssistor\Chorn.AiPullRequestAssistor.csproj
    - name: Package
      run: dotnet publish --framework net7 --no-restore --no-build --configuration Release --verbosity normal -o Release/win-x64 Chorn.AiPullRequestAssistor\Chorn.AiPullRequestAssistor.csproj
    - name: Upload Build Artifact
      uses: actions/upload-artifact@v3
      with:
        name: AiPullRequestAssistor-net7-winx64
        path: Release/win-x64
